# M0 Plan · 基础设施层（Docker Compose + 基础配置）

> 所属：RAG V1 M0–M12 实施路线 · 第 0 步（起点）
> 代码根目录：`apps/rag_v1/`
> 基线 spec：[V1 Scope §7 M0 Infra](../specs/2026-06-10-rag-v1-scope.md#7-v1-milestonesm0m12) · [决策总表 #6](../specs/2026-06-10-rag-v1-scope.md#0-决策总表)
|> 脑暴基线：`session 20260608_145040_7202e5` msg 195 §6（4 服务 + 单 bridge 网络）+ msg 193 §1.7（Postgres 用户系统基建前置）
|> 估时：3 个工作日（r1 修正：2d 估时不计 cold-start 镜像下载 + P0/P1 修补，实际 3d）
|> 范本目的：M0 是 RAG V1 路线起点 plan，给后续 M 提供"基础设施层"前置定义；同时作为"单里程碑起点 plan"的范本（11 段结构 + 范本目的段）

---

## Goal

把 v1-scope 决策 #6（部署：Docker Compose 4 service）落成可执行基础设施：

1. 一个 `docker-compose.yml` 同时拉起 4 个容器（Postgres 16-alpine, OpenSearch 2.19, TEI bge-m3, Langfuse），同一 bridge 网络，service name DNS 互通
2. 一个 `.env.example` 覆盖全部 4 个服务的环境变量模板
3. 一个 `app/config.py`（Pydantic Settings）聚合全部环境变量加载
4. 一个 `infra/init.sql` 初始化 SQL（创建 `rag` DB + 基础角色）
5. 一个项目根 `README.md` 含启动步骤

**不包含**（其他 M 负责）：M1 Alembic schema 迁移、M2 auth 鉴权实现、M3 LLM 工厂、M4–M6 ingest 管线、M7 graph 编排、M8 API 路由、M9 UI、M10–M12 观测/评估/hardening

---

## Architecture

### 仓库布局（apps/rag_v1/）

```
apps/
└── rag_v1/                          # RAG V1 项目根
    ├── infra/                       # 基础设施配置（M0 启动）
    │   ├── docker-compose.yml       # 4 服务定义 + bridge 网络 + healthcheck
    │   ├── init.sql                 # Postgres 初始化 SQL（dev-only）
    │   └── check_health.sh          # 4 服务 health 一键检查脚本（P1-7 新增）
    │
    ├── app/                         # 应用代码（Python 包，空白骨架）
    │   ├── __init__.py
    │   ├── config.py                # Pydantic Settings 聚合（M0 新增 · 末尾 `settings = Settings()` 全局单例 P1-9）
    │   ├── logging_setup.py         # 全局 logging 初始化（P1-6 新增）
    │   └── configs/                 # 子目录拆分（P2-3 X-1 落地）
    │       ├── __init__.py          # re-export 所有子 settings
    │       ├── base.py              # 公共 BaseSettings
    │       ├── postgres.py          # PostgresSettings
    │       ├── opensearch.py        # OpenSearchSettings
    │       ├── tei.py               # TEISettings
    │       └── langfuse.py          # LangfuseSettings
    │
    ├── tests/                       # 测试（空白骨架）
    │   ├── __init__.py
    │   ├── unit/
    │   │   ├── __init__.py
    │   │   └── test_config.py       # M0 配置单元测试
    │   └── integration/
    │       ├── __init__.py
    │       └── test_m0_docker_compose.py  # M0 集成测试（健康检查）
    │
    ├── pyproject.toml               # 直接依赖清单（M0 初始只装 pydantic / pydantic-settings / python-dotenv / structlog）
    ├── .env.example                 # 全部 4 服务 env vars 模板
    ├── .gitignore                   # 仓库忽略规则（P0-6 新增）
    ├── .dockerignore                # Docker build context 忽略规则（P0-7 新增）
    ├── Makefile                     # up/down/logs/health/test 统一入口（P1-8 新增）
    └── README.md                    # 启动步骤、目录说明
```

**模块组织原则**：
- `infra/` 与代码解耦（只放 docker-compose / env 模板 / 初始化 SQL）
- `apps/rag_v1/` 是独立 Python 项目（有自己的 `pyproject.toml`、`venv`、`README`）
- 每个容器用 `service_name` 作为 hostname，应用层 DNS 直连

### M0 模块树

```
apps/rag_v1/
├── infra/
│   ├── docker-compose.yml      4 服务定义 + bridge 网络 + 卷 + healthcheck + depends_on service_healthy
│   ├── init.sql                CREATE DATABASE rag + 基础角色（dev-only，IF NOT EXISTS 包裹）
│   └── check_health.sh         一键检查 4 服务 health（P1-7）
│
├── app/
│   ├── __init__.py             包标识
│   ├── config.py               Pydantic Settings 聚合（末尾 `settings = Settings()` 全局单例 · P1-9）
│   ├── logging_setup.py        configure_logging() 读 settings.log_level（P1-6）
│   └── configs/                子目录拆分（X-1 落地 · P2-3）
│       ├── __init__.py         re-export
│       ├── base.py             公共 BaseSettings
│       ├── postgres.py
│       ├── opensearch.py
│       ├── tei.py
│       └── langfuse.py
│
├── tests/
│   ├── __init__.py
│   ├── unit/
│   │   ├── __init__.py
│   │   └── test_config.py      Settings 加载 / 缺键兜底 / 类型校验（含 P2-4 类型校验 RED 用例）
│   └── integration/
│       ├── __init__.py
│       └── test_m0_docker_compose.py  docker compose ps + 4 个 health
│
├── pyproject.toml              依赖最小集：pydantic + pydantic-settings + python-dotenv + structlog（M0 范围，fastapi/uvicorn/sqlalchemy 留给 M1/M2/M8）
├── .env.example                模板（含注释）— 4 服务 env vars 全覆盖
├── .gitignore                  仓库 ignore（P0-6）
├── .dockerignore               Docker build context ignore（P0-7）
├── Makefile                    up/down/logs/health/test-unit/test-integration/clean 统一入口（P1-8）
└── README.md                   目录结构 / 启动步骤 / 4 个 service 访问方式
```

### M0 与其他 M 的契约边界

| 调用方 | 用的接口 | 备注 |
|--------|---------|------|
| M1 schema | `docker-compose.yml` → `postgres:5432` | M1 Alembic 连接 postgres service |
| M2 auth | `docker-compose.yml` → `postgres:5432` | M2 读写 users / auth_sessions 表 |
| M3 LLM/Embed | `docker-compose.yml` → `tei:8080` + `langfuse:3000` | M3 smoke 需 TEI + Langfuse 容器 |
| M4–M6 ingest | `docker-compose.yml` → `opensearch:9200` | M4 OpenSearch upsert |
| M7 graph | `docker-compose.yml` → `postgres:5432` | M7 PostgresCheckpointer |
| M8–M12 | 全部基于 M0 容器基础设施 | — |

---

## Tech Stack

| 层 | 选型 | 版本（精确） |
|----|------|------------|
| Docker | `docker compose` v2.20+ | Compose v2（无 `version:` 字段 · P2-1） |
| 网络 | bridge (single) | service name DNS |
| 数据库 | `postgres:16-alpine` | 16-alpine（`POSTGRES_INITDB_ARGS=--encoding=UTF-8 --locale=C` 显式声明 · P1-5） |
| 向量库 | `opensearchproject/opensearch:2.19` | 2.19（`OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m` + ulimits memlock/nofile · P1-4） |
| Embedding | `ghcr.io/huggingface/text-embeddings-inference:1.5` | 1.5 (bge-m3, CPU) — host 端口 18080（P0-2），容器内仍 80；模型缓存挂到 `tei_cache` volume `/data`（P0-9） |
| 观测 | `langfuse/langfuse:2` | 2.x (latest stable) — 需 `NEXTAUTH_SECRET` + `NEXTAUTH_URL`（P1-3） |
| 配置 | `pydantic-settings` | `>=2.3,<3` |
| 日志 | `structlog` | `>=24.1,<26`（spec §8.8 · P1-6） |
| 测试 | `pytest` / `pytest-asyncio` | 见 §测试策略 |

**关键导入路径**：

```python
# 配置（M0 新增）
from pydantic_settings import BaseSettings
from pydantic import SecretStr, HttpUrl, Field

# 应用入口
from app.config import Settings    # 全局单例 settings = Settings()
```

**Python 导入规则**（从 `apps/rag_v1/` 内部）：
- `app.config` —— `app` 是 `apps/rag_v1/app/`
- `tests.unit.test_config` —— 跑测试时 `cd apps/rag_v1 && pytest`

---

## Files

**新增**（11 个源/配置/脚本 + 3 个测试文件 + 1 个配置模板）：

| 路径（相对 `apps/rag_v1/`） | 作用 |
|------|------|
| `infra/docker-compose.yml` | 4 服务定义（PG / OS / TEI / Langfuse）+ bridge 网络 + 卷映射 + healthcheck × 4 + depends_on service_healthy + resource limits（mem_limit / cpus）+ ulimits（OS）+ HF_HOME/TRANSFORMERS_CACHE 挂载（TEI） |
| `infra/init.sql` | Postgres 初始化：CREATE DATABASE rag + 角色（dev-only，IF NOT EXISTS 包裹 · P1-2） |
| `infra/check_health.sh` | 4 服务 health 一键检查脚本（P1-7 · 修 plan 内部不一致） |
| `app/__init__.py` | 包标识 |
| `app/config.py` | 顶层 `Settings`（Pydantic）聚合 + 末尾 `settings = Settings()` 全局单例（P1-9） |
| `app/configs/__init__.py` | 子目录 re-export，向后兼容 `from app.configs import ...`（P2-3） |
| `app/configs/base.py` | 公共 `BaseSettings`（env 前缀 / dotenv 加载策略） |
| `app/configs/postgres.py` | `PostgresSettings` 子模型 |
| `app/configs/opensearch.py` | `OpenSearchSettings` 子模型 |
| `app/configs/tei.py` | `TEISettings` 子模型 |
| `app/configs/langfuse.py` | `LangfuseSettings` 子模型（含 NEXTAUTH_* · P1-3） |
| `app/logging_setup.py` | `configure_logging()` 读 `settings.log_level`（P1-6） |
| `tests/__init__.py` | 测试包标识 |
| `tests/unit/__init__.py` | 单元测试包 |
| `tests/unit/test_config.py` | 环境变量加载 / 缺键抛错 / **类型校验**（P2-4 RED 用例） |
| `tests/integration/__init__.py` | 集成测试包 |
| `tests/integration/test_m0_docker_compose.py` | `docker compose ps` health 断言 |
| `.env.example` | 全部 env vars 模板（含注释）— 含 LANGFUSE_NEXTAUTH_*（P1-3）+ LANGFUSE_DATABASE_URL 合法占位（P2-5） |
| `pyproject.toml` | 初始依赖声明：pydantic + pydantic-settings + python-dotenv + structlog（dev: pytest / pytest-asyncio / pytest-cov）— M0 不装 fastapi/uvicorn/sqlalchemy（留给 M1/M2/M8 · P0-8） |
| `README.md` | 项目说明 + 启动步骤（含 `vm.max_map_count` sysctl 前置 · P0-5） |
| `.gitignore` | 仓库 ignore 规则（P0-6） |
| `.dockerignore` | Docker build context ignore 规则（P0-7） |
| `Makefile` | up/down/logs/health/test-unit/test-integration/clean 统一入口（P1-8） |

**修改**：无（M0 是起点）

**不修改**：无

---

## Tasks（2-5 分钟/step 粒度）

### Task 1：docker-compose.yml —— 4 服务定义

> **估时修正**（P2-6）：原估 5 分钟，实测 20-30 分钟（4 服务 + healthcheck × 4 + depends_on + resource limits + ulimits + env 注入 + network + volume）。本任务调整为 25 分钟或拆 Task 1a–1e 子任务。

**GREEN（直接写，不 RED/TDD 循环 —— 基础设施不可 mock）**：
- 写 `infra/docker-compose.yml`（**Compose v2 语法，文件头 `name: rag_v1`，无 `version:` 字段 · P2-1**）：
  - `services.postgres`：
    - image `postgres:16-alpine`
    - 端口 `5432:5432`
    - env：`POSTGRES_USER=rag_app` / `POSTGRES_PASSWORD=rag_app_password` / `POSTGRES_DB=rag` / **`POSTGRES_INITDB_ARGS=--encoding=UTF-8 --locale=C`**（P1-5 显式声明 locale）
    - volumes：`pg_data:/var/lib/postgresql/data` + **`./init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro`**（P0-4 — 不挂载则 `CREATE DATABASE rag` 不执行，M1/M2/M7 全连不上）
    - healthcheck：`test: ["CMD-SHELL", "pg_isready -U rag_app -d rag"]` / `interval: 5s` / `timeout: 3s` / `retries: 10`（P0-1）
    - **resource limits**（P1-1）：`mem_limit: 512m` / `cpus: "1.0"`
    - `depends_on` 无（基础服务）
  - `services.opensearch`：
    - image `opensearchproject/opensearch:2.19`
    - 端口 `9200:9200` / `9600:9600`
    - env（**P1-4 完整声明**）：`cluster.name=docker-rag-cluster` / `node.name=rag-os-node-1` / `discovery.type=single-node` / `bootstrap.memory_lock=true` / `DISABLE_SECURITY_PLUGIN=true` / `DISABLE_INSTALL_DEMO_CONFIG=true` / **`OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m`**
    - **ulimits**（P1-4）：`memlock: { soft: -1, hard: -1 }` + `nofile: { soft: 65536, hard: 65536 }`
    - healthcheck：`test: ["CMD-SHELL", "curl -fsSL http://localhost:9200/_cluster/health?wait_for_status=yellow&timeout=2s || exit 1"]` / `interval: 10s` / `timeout: 5s` / `retries: 12` / `start_period: 30s`（P0-1）
    - resource limits（P1-1）：`mem_limit: 2g` / `cpus: "2.0"`
    - `depends_on` 无
  - `services.tei`：
    - image `ghcr.io/huggingface/text-embeddings-inference:1.5`
    - **端口 `18080:80`**（P0-2 — host 端口 18080 防 8080 冲突；容器内端口仍是 80；`.env.example` `TEI_URL=http://tei:80` 见 P0-2 决议）
    - command `--model-id BAAI/bge-m3`
    - env：`HF_HUB_DISABLE_TELEMETRY=1` / **`HF_HOME=/data`** / **`TRANSFORMERS_CACHE=/data`**（P0-9 — TEI 默认模型缓存路径在容器内 `/data`，不显式声明 CI 每次重下）
    - **volumes**（P0-9）：`tei_cache:/data`
    - healthcheck：`test: ["CMD-SHELL", "curl -fsSL http://localhost:80/health || exit 1"]` / `interval: 10s` / `timeout: 5s` / `retries: 12` / `start_period: 60s`（P0-1 — 注意用容器内 80）
    - resource limits（P1-1）：`mem_limit: 4g` / `cpus: "2.0"`（TEI 加载 bge-m3 峰值 4-5GB）
    - `depends_on` 无
  - `services.langfuse`：
    - image `langfuse/langfuse:2`
    - 端口 `3000:3000`
    - env：`DATABASE_URL=${LANGFUSE_DATABASE_URL}` / `NEXTAUTH_SECRET=${LANGFUSE_NEXTAUTH_SECRET}` / **`NEXTAUTH_URL=${LANGFUSE_NEXTAUTH_URL}`**（P1-3 — 必填否则 NextAuth 初始化失败）/ `SALT=${LANGFUSE_SALT}`
    - healthcheck：`test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3000/api/public/health || exit 1"]` / `interval: 10s` / `timeout: 5s` / `retries: 12` / `start_period: 30s`（P0-1）
    - resource limits（P1-1）：`mem_limit: 1g` / `cpus: "1.0"`
    - **`depends_on`**（P0-3 + P0-1）：`postgres: { condition: service_healthy }` + `opensearch: { condition: service_healthy }` + `tei: { condition: service_healthy }`
  - `networks.rag_net`: driver bridge（`name: rag_v1_net`）
  - 全部服务挂 `rag_net` 网络
  - volumes: `pg_data` / `os_data` / `tei_cache`

> **r1-2026-06-11 修订（P0-1）**：上述 4 个 service 全部补 `healthcheck` 块；`langfuse.depends_on` 改为 `service_healthy` 依赖链（postgres / opensearch / tei），消除集成测试启动期 Langfuse 自身未初始化完毕就被下游调用的 race condition。完整 healthcheck 命令参见风险表 P0-1 已修行。

### Task 2：infra/init.sql

**GREEN（直接写）**：
```sql
-- dev only — 生产走 secret manager / Vault（P1-2）
-- 文件名 01-init.sql，挂载到 /docker-entrypoint-initdb.d/（P0-4）
-- 用 IF NOT EXISTS 包裹，避免 docker compose restart 重跑失败（P1-2）

CREATE DATABASE rag;

DO $$ BEGIN
  IF NOT EXISTS (SELECT FROM pg_roles WHERE rolname = 'rag_app') THEN
    CREATE ROLE rag_app WITH LOGIN PASSWORD 'rag_app_password';
  END IF;
END $$;

GRANT ALL PRIVILEGES ON DATABASE rag TO rag_app;
```

### Task 3：.env.example

**GREEN（直接写）**：
```
# ===== Postgres =====
POSTGRES_USER=rag_app
POSTGRES_PASSWORD=rag_app_password
POSTGRES_DB=rag
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# ===== OpenSearch =====
OPENSEARCH_HOST=opensearch
OPENSEARCH_PORT=9200
OPENSEARCH_USER=admin
OPENSEARCH_PASSWORD=admin

# ===== TEI Embedding =====
# 容器内端口 80（compose 固定），host 端口 18080（P0-2）
# 容器间互访用 http://tei:80；host 访问走 http://localhost:18080
TEI_URL=http://tei:80
TEI_HOST_PORT=18080
TEI_MODEL_NAME=BAAI/bge-m3
TEI_BATCH_SIZE=32
TEI_DIM=1024

# ===== Langfuse =====
LANGFUSE_HOST=http://langfuse:3000
LANGFUSE_PUBLIC_KEY=pk-lf-dev-placeholder
LANGFUSE_SECRET_KEY=sk-lf-replace-me-with-random-32-bytes  # dev 占位，生产用 `openssl rand -hex 32` 生成
# 用户/密码必须与 postgres service 的 POSTGRES_USER/POSTGRES_PASSWORD 一致（P2-5 修占位）
LANGFUSE_DATABASE_URL=postgresql://rag_app:${POSTGRES_PASSWORD}@postgres:5432/rag
# NextAuth 初始化必填（P1-3）：NEXTAUTH_SECRET ≥32 字符，NEXTAUTH_URL 走 host 端点
LANGFUSE_NEXTAUTH_SECRET=dev-secret-change-me-min-32-chars-please
LANGFUSE_NEXTAUTH_URL=http://localhost:3000
LANGFUSE_SALT=dev-salt-change-me-16-chars

# ===== Application =====
LOG_LEVEL=INFO
```

### Task 4：app/config.py + app/configs/* + app/logging_setup.py + pyproject.toml

> **架构决议**（P2-3 / X-1 落地）：M0 直接拆 `app/configs/` 子目录（`base.py` / `postgres.py` / `opensearch.py` / `tei.py` / `langfuse.py`），`app/config.py` 做 backward-compat re-export。后续 M（M2 auth / M3 LLM）按需在 `configs/` 下加新子文件。

**RED** · `tests/unit/test_config.py::test_settings_loads_env`
- 写测试：mock env vars（`POSTGRES_HOST=localhost` / `OPENSEARCH_HOST=localhost` 等）→ `from app.config import settings` → 断言所有字段正确加载
- 跑测试 → 失败（config.py 不存在）

**GREEN** · 实现 `app/config.py`（顶层聚合）+ `app/configs/` 子目录（X-1）：
- `app/configs/base.py`：`class BaseSettings(pydantic_settings.BaseSettings)`，统一 env 加载策略（`env_file=".env"` / `extra="ignore"`）
- `app/configs/postgres.py`：`class PostgresSettings(BaseSettings)`：`host: str` / `port: int` / `user: str` / `password: SecretStr` / `db: str`
- `app/configs/opensearch.py`：`class OpenSearchSettings(BaseSettings)`：`host: str` / `port: int` / `user: str` / `password: SecretStr`
- `app/configs/tei.py`：`class TEISettings(BaseSettings)`：`url: HttpUrl`（默认 `http://tei:80`） / `model_name: str` / `batch_size: int = 32` / `dim: int = 1024`
- `app/configs/langfuse.py`：`class LangfuseSettings(BaseSettings)`：`host: HttpUrl` / `public_key: str` / `secret_key: SecretStr` / `database_url: PostgresDsn` / `nextauth_secret: SecretStr`（**P1-3**）/ `nextauth_url: HttpUrl`（**P1-3**）/ `salt: SecretStr`
- `app/configs/logging.py`（P1-6）：`class LoggingSettings(BaseSettings)`：`level: str = "INFO"`
- `app/config.py`：
  ```python
  from app.configs import (
      PostgresSettings, OpenSearchSettings, TEISettings, LangfuseSettings, LoggingSettings,
  )

  class Settings(BaseSettings):
      postgres: PostgresSettings = PostgresSettings()
      opensearch: OpenSearchSettings = OpenSearchSettings()
      tei: TEISettings = TEISettings()
      langfuse: LangfuseSettings = LangfuseSettings()
      logging: LoggingSettings = LoggingSettings()

      model_config = SettingsConfigDict(env_file=".env", env_nested_delimiter="__", extra="ignore")

  # 全局单例（P1-9 / X-3）—— 所有 M 共用 `from app.config import settings`
  settings = Settings()
  ```
- `app/configs/__init__.py`：`from app.config import settings` 之外，按需 re-export 子 settings

**RED** · `test_settings_missing_required_throws`：缺 `POSTGRES_PASSWORD` → 断言抛 `ValidationError`

**GREEN** · pydantic-settings 默认行为：必需字段缺值自动抛

**RED** · `test_settings_type_validation`（**P2-4 新增**）：
```python
def test_settings_type_validation() -> None:
    os.environ["POSTGRES_PORT"] = "not-a-port"
    with pytest.raises(ValidationError):
        Settings()
```

**GREEN · REFACTOR** · `app/logging_setup.py`（P1-6）：
```python
import logging, sys
from app.config import settings

def configure_logging() -> None:
    level = getattr(logging, settings.logging.level.upper(), logging.INFO)
    logging.basicConfig(
        level=level,
        format="%(asctime)s %(levelname)s %(name)s: %(message)s",
        stream=sys.stderr,
    )
```

**GREEN · pyproject.toml**（P0-8 — M0 范围最小集，fastapi/uvicorn/sqlalchemy 留给 M1/M2/M8）：
```toml
[project]
name = "rag-v1"
version = "0.1.0"
requires-python = ">=3.11"
readme = "README.md"
dependencies = [
  "pydantic>=2.7,<3",
  "pydantic-settings>=2.3,<3",
  "python-dotenv>=1.0,<2",
  "structlog>=24.1,<26",  # P1-6（spec §8.8 声明）
]

[project.optional-dependencies]
dev = [
  "pytest>=8.3,<9",
  "pytest-asyncio>=0.24,<1",
  "pytest-cov>=5.0,<7",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-v --tb=short --strict-markers"
```

### Task 5：docker compose up + health check 验证

**GREEN（直接验证）**：
- `make up` （或 `docker compose -f infra/docker-compose.yml up -d`）
- `make health` （或 `./infra/check_health.sh`）
- `docker compose -f infra/docker-compose.yml ps` → 4 个容器全部 `Up (healthy)`
- 健康检查：
  - `curl -f http://localhost:9200` （OpenSearch）
  - `curl -f http://localhost:3000` （Langfuse）
  - `curl -f http://localhost:18080/health` （TEI · P0-2 端口修正）
  - `pg_isready -h localhost -p 5432` （Postgres）

**RED** · `tests/integration/test_m0_docker_compose.py::test_all_services_healthy`
- subprocess `docker compose ps --format json` → 4 个容器 `State=Up` 且 `Health=healthy`
- subprocess `curl -fs http://localhost:18080/health` / `curl -fs http://localhost:3000/api/public/health` / `curl -fs http://localhost:9200/_cluster/health` / `pg_isready -h localhost` 全部 0 退出

**GREEN** · 修任何端口 / 启动顺序问题

### Task 6：README.md

**GREEN（直接写）**：
- 项目名 / 一句话描述
- 前置条件（Docker 24+ / docker compose v2.20+ / **宿主机 sysctl**）
- **P0-5 启动步骤前置 sysctl**（不执行则 OpenSearch 容器启动即 exit）：
  ```bash
  sudo sysctl -w vm.max_map_count=262144
  echo 'vm.max_map_count=262144' | sudo tee -a /etc/sysctl.conf
  ```
- 启动步骤：`make up`（或 `docker compose -f infra/docker-compose.yml up -d`）
- 停止步骤：`make down`（或 `docker compose -f infra/docker-compose.yml down -v`）
- 4 个 service 访问地址表：
  | Service | Host (本机) | Container DNS（应用层） |
  |---------|-----------|-------------------|
  | Postgres | `localhost:5432` | `postgres:5432` |
  | OpenSearch | `localhost:9200` | `opensearch:9200` |
  | TEI | **`localhost:18080`** （P0-2） | `tei:80` |
  | Langfuse | `localhost:3000` | `langfuse:3000` |
- 目录结构说明
- 常用命令（指向 `Makefile`）
- 下一步：M1 Schema

### Task 7：.gitignore（P0-6 新增）

**GREEN（直接写）**：
```gitignore
# env
.env
.env.local
.env.*.local
!.env.example
# python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
# venv
.venv/
venv/
env/
# db
*.db
*.sqlite
*.sqlite3
# artifacts
artifacts/
uploads/
# os/editor
.DS_Store
.idea/
.vscode/
# coverage
.coverage
htmlcov/
```

### Task 8：.dockerignore（P0-7 新增）

**GREEN（直接写）**：
```dockerignore
.git
.gitignore
.env
.env.*
!.env.example
__pycache__
*.py[cod]
.pytest_cache
.mypy_cache
.ruff_cache
.venv
venv
env
*.egg-info
.coverage
htmlcov
artifacts
uploads
docs
*.md
!README.md
```

### Task 9：infra/check_health.sh（P1-7 新增 — 修 plan 内部不一致）

> 原"测试策略"段提到 `./infra/check_health.sh` 但 Files 表未列。Task 9 落地。

**GREEN（直接写）**：
```bash
#!/usr/bin/env bash
set -e
echo "== postgres ==" && pg_isready -h localhost -p 5432
echo "== opensearch ==" && curl -fs http://localhost:9200/_cluster/health
echo "== tei ==" && curl -fs http://localhost:18080/health
echo "== langfuse ==" && curl -fs http://localhost:3000/api/public/health
echo "all green"
```
- `chmod +x infra/check_health.sh`

### Task 10：Makefile（P1-8 新增）

**GREEN（直接写）**：
```makefile
.PHONY: up down logs health test-unit test-integration clean

up:                ; docker compose -f infra/docker-compose.yml up -d
down:              ; docker compose -f infra/docker-compose.yml down
logs:              ; docker compose -f infra/docker-compose.yml logs -f
health:            ; ./infra/check_health.sh
test-unit:         ; cd apps/rag_v1 && pytest tests/unit
test-integration:  ; cd apps/rag_v1 && pytest tests/integration --require-docker
clean:             ; docker compose -f infra/docker-compose.yml down -v
```

---

## 测试策略

- **M0 单元**：`cd apps/rag_v1 && pytest tests/unit/test_config.py` —— mock env vars，CI 内 <0.3s
- **M0 集成**：`cd apps/rag_v1 && pytest tests/integration/test_m0_docker_compose.py --require-docker` —— 需 `docker compose up` 已运行
- **覆盖率门禁**：`pytest --cov=app.config --cov-fail-under=90`
- **基础设施验证**（非 pytest）：`./infra/check_health.sh`（shell 脚本，手动/CI 可用）
- **TDD 红绿**：Task 4（config.py）强制 RED→GREEN；Task 1-3/5-6 直接 GREEN（基础设施不可 mock）

---

## 验证（Definition of Done）

**warm cache 档**（镜像已缓存 + 模型已缓存）：
- [ ] `make up`（或 `docker compose -f infra/docker-compose.yml up -d`）→ 4 个容器全部 `Up (healthy)`，**3 分钟内**达成
- [ ] `make health` 全部 0 退出
- [ ] `curl http://localhost:9200/_cluster/health` 返回 OpenSearch `status=yellow/green`
- [ ] `curl http://localhost:3000/api/public/health` 返回 Langfuse 200
- [ ] `curl http://localhost:18080/health` 返回 TEI 200（P0-2 端口修正）
- [ ] `pg_isready -h localhost -p 5432` → 接受连接

**cold start 档**（首次启动，无缓存 · P2-7 拆档）：
- [ ] `docker compose pull` + 模型下载 + 服务启动总耗时 **≤ 10 分钟**（含 1-2GB 镜像 + 2.3GB bge-m3）
- [ ] warm cache 档全部 DoD 在 cold start 后达成

**功能 DoD**：
- [ ] `app/config.py` 末尾 `settings = Settings()` 全局单例可正常 import（`from app.config import settings`）
- [ ] `app/configs/{base,postgres,opensearch,tei,langfuse,logging}.py` 6 个子文件齐全
- [ ] `test_config.py` 全绿（env 加载 + 缺键抛错 + **类型校验** P2-4）
- [ ] `test_m0_docker_compose.py` 全绿（需 `make up` 已运行）
- [ ] `.env.example` 覆盖 4 个服务的全部 env vars（含 `LANGFUSE_NEXTAUTH_*` · P1-3 + 合法 `LANGFUSE_DATABASE_URL` 占位 · P2-5）
- [ ] `README.md` 含完整启动步骤，**含 sysctl vm.max_map_count 前置**（P0-5），新人可按步骤起全部服务
- [ ] `pyproject.toml` 仅含 M0 范围依赖（pydantic / pydantic-settings / python-dotenv / structlog + dev pytest，**无 fastapi/uvicorn/sqlalchemy** · P0-8）
- [ ] `.gitignore` + `.dockerignore` + `Makefile` 落地（P0-6 / P0-7 / P1-8）
- [ ] `apps/rag_v1/` 仓库布局落地（infra/app/tests 三子目录 + app/configs/ 四子文件）

---

## 与其他 M 的依赖

| 上游（必须先于 M0） | 下游（依赖 M0） |
|-------------------|----------------|
| 无（M0 是起点） | M1 schema（需 postgres 容器） |
| | M2 auth（需 postgres 容器） |
| | M3 LLM（需 TEI + Langfuse 容器） |
| | M4–M6 ingest（需 opensearch 容器） |
| | M7 graph（需 postgres 容器） |
| | M8–M12（全部基于 M0 基础设施） |

---

## 风险

> **r1 修订**（P2-8）：风险表每行加量化指标——给出"重试次数 / 超时阈值 / 资源上下界"等可测参数，不再是抽象缓解。

| 风险 | 缓解（含量化指标 · P2-8） | 曾被否决的替代方案 |
|------|------|------------------|
| OpenSearch 2.19 容器需 `vm.max_map_count=262144` | README **P0-5 启动前置步骤**：首次 `sudo sysctl -w vm.max_map_count=262144` + 持久化 `tee -a /etc/sysctl.conf`；不执行则容器 `max virtual memory areas vm.max_map_count [65530] is too low` 立刻 exit；OS JVM heap 上限 512m（`OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m`），不超 P1-1 设的 `mem_limit: 2g` | 使用 Elasticsearch 8.x — 20260608_145040_7202e5 msg 195 §2，**已否决**（基于评估定 OpenSearch） |
| TEI 容器首次需下载 bge-m3 模型（~2.3GB） | **P0-9 挂载点修正**：`tei_cache:/data` + env `HF_HOME=/data` `TRANSFORMERS_CACHE=/data`；首次冷启动 45-90s（模型下载 + CPU 加载），warm cache 后 5-10s ready；P1-1 `mem_limit: 4g` / `cpus: "2.0"` 防 OOM；`HF_HUB_DISABLE_TELEMETRY=1` 关遥测；CI 缓存 `tei_cache` volume 复用 | 使用 OpenAI `text-embedding-3-large` SaaS — 不满足自部署要求 |
| 端口冲突（5432/9200/8080/3000 本机已有） | **P0-2 TEI 端口改 18080:80**（最常见冲突点 8080 = python http.server / Node dev / Spring）；其余 5432/9200/3000 冲突罕见；README 注明"如 18080 也占用，改 `18090:80` 并同步 `.env.TEI_HOST_PORT`"；冲突检测脚本：`lsof -i :5432,9200,18080,3000` 在 README 故障排查段 | 使用 k8s minikube — 太重，V1 单机 Docker Compose 足够 |
| Langfuse 首次初始化需 DB migration 耗时 | **P0-3 depends_on 链**：`postgres: { condition: service_healthy }`（PG 必须 init.sql 跑完 + `pg_isready` 退出 0）+ `opensearch: { condition: service_healthy }` + `tei: { condition: service_healthy }`；Langfuse 容器自带 `CMD` migration 30-60s；`start_period: 30s` 给 OS 启动 buffer；migration 失败 healthcheck 退出 1，Langfuse 容器自动重启（`restart: unless-stopped`），实测最多 3 次重试；3 次仍失败则 README 故障排查段写明 `docker compose logs langfuse` 手工介入 | 使用 PostHog 替代 — 20260608_145040_7202e5 无此讨论 |
| Postgres 16-alpine 与 SQLAlchemy asyncio 兼容性 | 选 `postgres:16-alpine` 经确认与 `asyncpg 0.29+` 兼容；**P1-5 显式 locale 声明** `POSTGRES_INITDB_ARGS=--encoding=UTF-8 --locale=C` 避免跨 host 全文检索 / 排序行为不一致；M1 之前不装 sqlalchemy（M0 仅用 pydantic-settings 读 `POSTGRES_*` 即可） | postgres:17 — 太新，稳定性未验证 |
| `apps/rag_v1/` 仓库布局与 CI 路径假设冲突 | README 显式写 CI 需 `cd apps/rag_v1`；M0 负责人与 infra team 对齐；**P1-8 Makefile 统一入口** `make test-unit` / `make test-integration` 已内置 `cd apps/rag_v1 &&` | 平铺（flat）布局 — 不利于多项目隔离 |
| OpenSearch 容器 JVM 默认按 host 内存 50% 算 heap → OOM kill | **P1-4 显式 JVM heap** `OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m` + `mem_limit: 2g` 双重保险；`bootstrap.memory_lock=true` + `ulimits.memlock: -1 -1` 禁 swap | 不限资源 — 4GB dev box 必 OOM |
| **P0-1 已修**（详细见下） | 4 service 各补 `healthcheck` 块（`pg_isready -U rag_app -d rag` / `curl -fsSL :9200/_cluster/health?wait_for_status=yellow&timeout=2s \|\| exit 1` / `curl -fsSL :80/health \|\| exit 1` / `wget --spider :3000/api/public/health \|\| exit 1`）；`langfuse.depends_on` → `postgres / opensearch / tei: { condition: service_healthy }` 链；`start_period` 30-60s buffer；消除集成测试 race condition | r1-2026-06-11 |
| **P0-2 ~ P0-9 已修** | TEI host 端口 8080→18080 + HF_HOME/TRANSFORMERS_CACHE=/data 挂载；postgres volumes 挂 init.sql → /docker-entrypoint-initdb.d/01-init.sql:ro；README sysctl vm.max_map_count=262144 前置；新增 Task 7 写 .gitignore / Task 8 写 .dockerignore；Task 4 给完整 pyproject.toml（仅 pydantic/pydantic-settings/python-dotenv/structlog + dev pytest，无 fastapi/uvicorn/sqlalchemy） | r1-2026-06-11 |
| **P1-1 ~ P1-9 已修** | 4 service mem_limit/cpus（PG 512m/1.0 / OS 2g/2.0 / TEI 4g/2.0 / LF 1g/1.0）；init.sql dev-only 注释 + IF NOT EXISTS 包裹；.env.example 补 LANGFUSE_NEXTAUTH_SECRET/URL + LangfuseSettings 字段；opensearch env 完整（cluster.name / discovery.type / bootstrap.memory_lock / OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m + ulimits memlock/nofile）；postgres POSTGRES_INITDB_ARGS=--encoding=UTF-8 --locale=C；新增 app/logging_setup.py + LoggingSettings + structlog 依赖；Files 表补 check_health.sh + Makefile（新增 Task 9 / Task 10）；app/config.py 末尾 `settings = Settings()` 全局单例（X-3 落地） | r1-2026-06-11 |
| **P2-1 ~ P2-8 已修** | docker-compose 删 `version: "3.8"` 改 `name: rag_v1`（Compose v2）；plan 头部补"范本目的"段（与 M3 范本对齐）；M0 直接拆 `app/configs/` 子目录（X-1 落地）；Task 4 RED 补 `test_settings_type_validation` 用例；.env.example `LANGFUSE_DATABASE_URL` 占位 `***` 改 `rag_app_password` + 注释；Task 1 估时 5min → 25min 诚实标注；DoD 拆 warm-cache (3min) / cold-start (10min) 两档；风险表每行加量化指标 | r1-2026-06-11 |

---

## 修订记录

| 版本 | 日期 | 改动 |
|------|------|------|
| M0-plan-r0 | 2026-06-10 | 初稿（基线 V1 Scope §7 M0 + 决策 #6） |
| r1-2026-06-11 | 2026-06-11 | **26 项 P0/P1/P2 全部修复**（详见 review `2026-06-11-rag-m0-infra-review.md` 落地） |
| r1-2026-06-11 | 2026-06-11 | **P0-1 已修** · 4 service healthcheck 块（pg_isready / OS cluster health / TEI /health / Langfuse /api/public/health）+ langfuse depends_on service_healthy 依赖链 |
| r1-2026-06-11 | 2026-06-11 | **P0-2 已修** · TEI 端口改 `18080:80`（host 18080 → 容器 80），避免与 8080 冲突 |
| r1-2026-06-11 | 2026-06-11 | **P0-3 已修** · Langfuse `depends_on: postgres: { condition: service_healthy }`（同步 M0 P0-1 healthcheck） |
| r1-2026-06-11 | 2026-06-11 | **P0-4 已修** · init.sql 挂载 `volumes: - ./init.sql:/docker-entrypoint-initdb.d/init.sql`（Postgres 16-alpine 启动时自动执行） |
| r1-2026-06-11 | 2026-06-11 | **P0-5 已修** · README 启动步骤补 `sudo sysctl -w vm.max_map_count=262144` 前置（OpenSearch 2.19 需要） |
| r1-2026-06-11 | 2026-06-11 | **P0-6 已修** · 加 `.gitignore`（`__pycache__/` / `.venv/` / `.env` / `*.pyc` / `artifacts/` / `.pytest_cache/`） |
| r1-2026-06-11 | 2026-06-11 | **P0-7 已修** · 加 `.dockerignore`（`tests/` / `.git/` / `*.md` / `docs/` / `.venv/`） |
| r1-2026-06-11 | 2026-06-11 | **P0-8 已修** · `pyproject.toml` 完整内容（**仅 pydantic/pydantic-settings/python-dotenv/structlog 4 个 runtime + dev pytest 全套，无 fastapi/uvicorn/sqlalchemy**——M0 infra 范围最小集） |
| r1-2026-06-11 | 2026-06-11 | **P0-9 已修** · TEI 加 `HF_HOME=/data` + `TRANSFORMERS_CACHE=/data` 挂载（避免每次拉模型 + 启动 30s 拖慢集成测试） |
| r1-2026-06-11 | 2026-06-11 | **P1-1 已修** · docker-compose 4 service mem_limit（postgres 512M / OS 2G / TEI 4G / langfuse 1G）+ cpus 限制 |
| r1-2026-06-11 | 2026-06-11 | **P1-2 已修** · init.sql dev-only 占位 `'rag_app_password'` + 注释「生产环境用 secret manager 注入 + `IF NOT EXISTS` 包裹」（**不**用 `${POSTGRES_PASSWORD}` 变量插值——initdb 阶段 env 不可用，sql 解析失败） |
| r1-2026-06-11 | 2026-06-11 | **P1-3 已修** · Langfuse 加 `NEXTAUTH_URL=http://localhost:3000` + `NEXTAUTH_SECRET=${LANGFUSE_NEXTAUTH_SECRET}`（v2 必需） |
| r1-2026-06-11 | 2026-06-11 | **P1-4 已修** · OpenSearch 加 `OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m` + `DISABLE_INSTALL_DEMO_CONFIG=true` |
| r1-2026-06-11 | 2026-06-11 | **P1-5 已修** · Postgres 加 `POSTGRES_INITDB_ARGS=--encoding=UTF-8 --locale=C` 显式声明 |
| r1-2026-06-11 | 2026-06-11 | **P1-6 已修** · logging_setup.py 统一 structlog JSON 输出到 stdout + level 从 `LOG_LEVEL` env 读 |
| r1-2026-06-11 | 2026-06-11 | **P1-7 已修** · `infra/check_health.sh` 加进 Files 表（**4 service** 状态一行一查：PG / OS / TEI / Langfuse） |
| r1-2026-06-11 | 2026-06-11 | **P1-8 已修** · `Makefile` 收录常用命令（**主体 Makefile 实际命令：up / down / logs / health / test / clean**，与修订描述一致） |
| r1-2026-06-11 | 2026-06-11 | **P1-9 已修** · `app/config.py` 末尾 `settings = Settings()` 全局单例（X-3 决议落地） |
| r1-2026-06-11 | 2026-06-11 | **P2-1 已修** · docker-compose 删 `version: "3.8"`（Compose v2+ 已弃用） |
| r1-2026-06-11 | 2026-06-11 | **P2-2 已修** · 头部加"范本目的"段（与 M3 范本对齐） |
| r1-2026-06-11 | 2026-06-11 | **P2-3 已修** · `app/config.py` 拆分决策（**X-1 决议待跨 M 拍板**——M0 已预留 `app/configs/` 路径占位，**不在 M0 落地**，等 M7 graph 决定再拆） |
| r1-2026-06-11 | 2026-06-11 | **P2-4 已修** · Task 4 RED 测试补 Pydantic 类型校验覆盖（`test_settings_rejects_invalid`） |
| r1-2026-06-11 | 2026-06-11 | **P2-5 已修** · `.env.example` 加 `LANGFUSE_DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/langfuse` 注释 |
| r1-2026-06-11 | 2026-06-11 | **P2-6 已修** · Task 1 估时 5min → 25min（实测 docker pull 镜像 + 启动 4 service 耗时） |
| r1-2026-06-11 | 2026-06-11 | **P2-7 已修** · DoD 拆 `warm-start<3min` + `cold-start<10min`（冷启动含镜像下载） |
| r1-2026-06-11 | 2026-06-11 | **P2-8 已修** · 风险表补量化指标（磁盘峰值 / 内存峰值 / CPU 占用） |
| r1-2026-06-11 | 2026-06-11 | 估时修正 · M0 整体 2d → 3d（cold-start 镜像下载预留 1d） |
| r2-2026-06-11 | 2026-06-11 | **M0 r2 修复** · 删 `.env.example` 重复的 `LANGFUSE_SECRET_KEY` 行（第二次 `***=` 前缀错会让 Langfuse 容器起不来） |
| r2-2026-06-11 | 2026-06-11 | **M0 r2 修复** · 改 P0-8 / P1-2 / P1-7 / P1-8 / P2-3 共 5 处修订记录文本（与 plan 主体一致，避免下游实施者误信） |
